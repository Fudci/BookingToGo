import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    // padding: 12,
    // backgroundColor: 'red',
    borderWidth: 1,
    borderColor: 'grey',
    // width: '100%',
    // borderRadius: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
